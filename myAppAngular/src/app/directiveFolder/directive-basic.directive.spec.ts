import { DirectiveBasicDirective } from './directive-basic.directive';

describe('DirectiveBasicDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveBasicDirective();
    expect(directive).toBeTruthy();
  });
});
